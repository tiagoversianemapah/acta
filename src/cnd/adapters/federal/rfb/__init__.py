"""Receita Federal: os arquivos que atendem o mesmo portal.

Agrupados porque sao um orgao so, ainda que em varias pecas: `cego` e `pj`
sao duas formas de dirigir o formulario de CNPJ (mouse real e Playwright),
`cego_pf` e o mesmo robo cego no formulario de CPF, e `matriz` e `pdf` sao
leitura compartilhada por eles. Ver ADR-003.
"""
