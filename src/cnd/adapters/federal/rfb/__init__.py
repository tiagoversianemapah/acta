"""Receita Federal: os quatro arquivos que atendem o mesmo portal.

Agrupados porque sao um orgao so, ainda que em quatro pecas: `cego` e `pj`
sao duas formas de dirigir o mesmo site (mouse real e Playwright), e `matriz`
e `pdf` sao leitura compartilhada por elas. Ver ADR-003.
"""
